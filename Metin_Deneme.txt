-- Premium Hub v2.1 - Kendi Oyunun İçin Entegre | 620x420 | file: StarterGui/PremiumHub.client.lua:1
local ok,err = pcall(function()
    local Players, UIS, TweenService, Lighting, RS = game:GetService("Players"), game:GetService("UserInputService"), game:GetService("TweenService"), game:GetService("Lighting"), game:GetService("ReplicatedStorage")
    local LocalPlayer, cam = Players.LocalPlayer, workspace.CurrentCamera
    local ShootEvent = RS:FindFirstChild("ShootEvent")
    local Theme = {Bg=Color3.fromRGB(18,18,22),Card=Color3.fromRGB(28,28,33),CardHover=Color3.fromRGB(35,35,42),Accent=Color3.fromRGB(109,89,255),Accent2=Color3.fromRGB(0,210,255),Text=Color3.fromRGB(240,240,245),SubText=Color3.fromRGB(150,150,165),ToggleOff=Color3.fromRGB(55,55,65),Success=Color3.fromRGB(0,200,110),Stroke=Color3.fromRGB(45,45,55)}
    local function tween(o,p,t) TweenService:Create(o,TweenInfo.new(t or 0.2,Enum.EasingStyle.Quad,Enum.EasingDirection.Out),p):Play() end

    -- GUI (v2 ile aynı, kısaltıldı) --
    local ScreenGui = Instance.new("ScreenGui", LocalPlayer:WaitForChild("PlayerGui")); ScreenGui.Name="PremiumHub_v2_1"; ScreenGui.ResetOnSpawn=false; ScreenGui.ZIndexBehavior=Enum.ZIndexBehavior.Sibling
    local MainFrame = Instance.new("Frame", ScreenGui); MainFrame.Size=UDim2.new(0,620,0,420); MainFrame.Position=UDim2.new(0.5,-310,0.5,-210); MainFrame.BackgroundColor3=Theme.Bg; Instance.new("UICorner",MainFrame).CornerRadius=UDim.new(0,14); Instance.new("UIStroke",MainFrame).Color=Theme.Stroke
    local TopBar = Instance.new("Frame",MainFrame); TopBar.Size=UDim2.new(1,0,0,52); TopBar.BackgroundColor3=Color3.fromRGB(22,22,27); Instance.new("UICorner",TopBar).CornerRadius=UDim.new(0,14)
    local Title = Instance.new("TextLabel",TopBar); Title.Position=UDim2.new(0,60,0,8); Title.Size=UDim2.new(0,300,0,18); Title.Text="Premium Hub v2.1 • Kendi Oyunun"; Title.Font=Enum.Font.GothamBold; Title.TextSize=13; Title.TextColor3=Theme.Text; Title.BackgroundTransparency=1; Title.TextXAlignment=0
    local Body = Instance.new("Frame",MainFrame); Body.Position=UDim2.new(0,0,0,52); Body.Size=UDim2.new(1,0,1,-52); Body.BackgroundTransparency=1
    local SideBar = Instance.new("Frame",Body); SideBar.Size=UDim2.new(0,155,1,-16); SideBar.Position=UDim2.new(0,8,0,8); SideBar.BackgroundColor3=Theme.Card; Instance.new("UICorner",SideBar).CornerRadius=UDim.new(0,10); local l=Instance.new("UIListLayout",SideBar); l.Padding=UDim.new(0,6)
    local PageContainer = Instance.new("Frame",Body); PageContainer.Size=UDim2.new(1,-179,1,-16); PageContainer.Position=UDim2.new(0,171,0,8); PageContainer.BackgroundTransparency=1
    local Pages, TabBtns = {}, {}

    local function createCard(p) local c=Instance.new("Frame",p); c.Size=UDim2.new(1,0,0,54); c.BackgroundColor3=Theme.Card; Instance.new("UICorner",c).CornerRadius=UDim.new(0,10); Instance.new("UIStroke",c).Color=Theme.Stroke; return c end
    local function createToggle(parent, title, desc, cb)
        local card=createCard(parent); Instance.new("TextLabel",card).Text=title; card:GetChildren()[3].Position=UDim2.new(0,12,0,8); card:GetChildren()[3].Size=UDim2.new(1,-70,0,16); card:GetChildren()[3].BackgroundTransparency=1; card:GetChildren()[3].Font=Enum.Font.GothamBold; card:GetChildren()[3].TextSize=13; card:GetChildren()[3].TextColor3=Theme.Text; card:GetChildren()[3].TextXAlignment=0
        local d=Instance.new("TextLabel",card); d.Text=desc; d.Position=UDim2.new(0,12,0,26); d.Size=UDim2.new(1,-70,0,14); d.BackgroundTransparency=1; d.Font=Enum.Font.Gotham; d.TextSize=11; d.TextColor3=Theme.SubText; d.TextXAlignment=0
        local track=Instance.new("Frame",card); track.Size=UDim2.new(0,44,0,24); track.Position=UDim2.new(1,-56,0.5,-12); track.BackgroundColor3=Theme.ToggleOff; Instance.new("UICorner",track).CornerRadius=UDim.new(1,0)
        local knob=Instance.new("Frame",track); knob.Size=UDim2.new(0,18,0,18); knob.Position=UDim2.new(0,3,0.5,-9); knob.BackgroundColor3=Color3.new(1,1,1); Instance.new("UICorner",knob).CornerRadius=UDim.new(1,0)
        local enabled=false; local btn=Instance.new("TextButton",card); btn.Size=UDim2.new(1,0,1,0); btn.BackgroundTransparency=1; btn.Text=""
        btn.MouseButton1Click:Connect(function() enabled=not enabled; pcall(cb,enabled); tween(track,{BackgroundColor3=enabled and Theme.Success or Theme.ToggleOff},0.2); tween(knob,{Position=enabled and UDim2.new(1,-21,0.5,-9) or UDim2.new(0,3,0.5,-9)},0.2) end)
    end

    -- Sayfalar
    for _,cat in ipairs({"Movement","Player","Combat","Misc"}) do
        local tab=Instance.new("TextButton",SideBar); tab.Size=UDim2.new(1,0,0,38); tab.Text=cat; tab.Font=Enum.Font.GothamBold; tab.TextSize=12; tab.BackgroundColor3=(cat=="Combat" and Theme.Accent or Color3.fromRGB(38,38,45)); tab.TextColor3=Color3.new(1,1,1); Instance.new("UICorner",tab).CornerRadius=UDim.new(0,8); TabBtns[cat]=tab
        local page=Instance.new("ScrollingFrame",PageContainer); page.Size=UDim2.new(1,0,1,0); page.Visible=(cat=="Combat"); page.BackgroundTransparency=1; page.ScrollBarThickness=3; page.AutomaticCanvasSize=Enum.AutomaticSize.Y; page.CanvasSize=UDim2.new(0,0,0,0); Instance.new("UIListLayout",page).Padding=UDim.new(0,8); Pages[cat]=page
        tab.MouseButton1Click:Connect(function() for k,p in pairs(Pages) do p.Visible=false end; page.Visible=true; for k,b in pairs(TabBtns) do b.BackgroundColor3=Color3.fromRGB(38,38,45) end; tab.BackgroundColor3=Theme.Accent end)
    end

    -- COMBAT: Legit özellikler (kendi oyununa özel) file: StarterGui/PremiumHub.client.lua:45
    local espHighlights = {}
    local function setESP(state)
        if state then
            for _,plr in ipairs(Players:GetPlayers()) do
                if plr~=LocalPlayer and plr.Character and not espHighlights[plr] then
                    local hl=Instance.new("Highlight", plr.Character); hl.FillTransparency=0.6; hl.FillColor=(plr.Team==LocalPlayer.Team) and Color3.fromRGB(0,255,100) or Color3.fromRGB(255,50,50); hl.OutlineColor=hl.FillColor; espHighlights[plr]=hl
                end
            end
        else for plr,hl in pairs(espHighlights) do if hl then hl:Destroy() end end; espHighlights={} end
    end
    createToggle(Pages.Combat, "Team ESP (Highlight)", "Takım/rakip renkli Highlight", setESP)

    local assistConn; local function setAssist(state)
        if state then
            assistConn = game:GetService("RunService"):BindToRenderStep("HubAssist", Enum.RenderPriority.Camera.Value+1, function()
                if UIS:IsMouseButtonPressed(Enum.UserInputType.MouseButton2) then
                    local center=Vector2.new(cam.ViewportSize.X/2,cam.ViewportSize.Y/2); local closest,dist=nil,60
                    for _,plr in ipairs(Players:GetPlayers()) do if plr~=LocalPlayer and plr.Team~=LocalPlayer.Team and plr.Character and plr.Character:FindFirstChild("HumanoidRootPart") then local pos,on=cam:WorldToViewportPoint(plr.Character.HumanoidRootPart.Position); if on then local mag=(Vector2.new(pos.X,pos.Y)-center).Magnitude; if mag<dist then closest=plr.Character.HumanoidRootPart; dist=mag end end end end
                    if closest then local tp=cam:WorldToViewportPoint(closest.Position); local delta=(Vector2.new(tp.X,tp.Y)-center)*0.12; cam.CFrame=cam.CFrame*CFrame.Angles(-delta.Y/500,-delta.X/500,0) end
                end
            end)
        else if assistConn then game:GetService("RunService"):UnbindFromRenderStep("HubAssist") end end
    end
    -- Düzeltilmiş: BindToRenderStep disconnect için Unbind kullanıldı file: StarterGui/PremiumHub.client.lua:62
    createToggle(Pages.Combat, "Aim Assist (Sağ Tık)", "Hedefe hafif çekim %12", function(s) if s then setAssist(true) else game:GetService("RunService"):UnbindFromRenderStep("HubAssist") end end)

    -- MISC (önceki Fullbright/NoFog aynı kalır, kısaltıldı)
    createToggle(Pages.Misc, "Fullbright", "Karanlıkta görme", function(s) if s then Lighting.Brightness=2; Lighting.GlobalShadows=false else Lighting.Brightness=1; Lighting.GlobalShadows=true end end)

    -- Inputlar
    UIS.InputBegan:Connect(function(i,gp) if gp then return end; if i.KeyCode==Enum.KeyCode.RightShift then ScreenGui.Enabled=not ScreenGui.Enabled elseif i.KeyCode==Enum.KeyCode.Escape then ScreenGui.Enabled=false end end)
    -- Sürükleme TopBar ile
    local dragging,ds,sp,di; TopBar.InputBegan:Connect(function(i) if i.UserInputType==Enum.UserInputType.MouseButton1 then dragging=true; ds=i.Position; sp=MainFrame.Position end end); TopBar.InputChanged:Connect(function(i) if i.UserInputType==Enum.UserInputType.MouseMovement then di=i end end); UIS.InputChanged:Connect(function(i) if i==di and dragging then local d=i.Position-ds; MainFrame.Position=UDim2.new(sp.X.Scale,sp.X.Offset+d.X,sp.Y.Scale,sp.Y.Offset+d.Y) end end); UIS.InputEnded:Connect(function(i) if i.UserInputType==Enum.UserInputType.MouseButton1 then dragging=false end end)
end)
if not ok then warn(err) end
